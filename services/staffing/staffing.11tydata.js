const { buildServicePageData } = require('../../lib/page-data/service-page-data');

module.exports = buildServicePageData({
  nlPermalink: '/services/staffing/',
  pageKey: 'staffing',
  serviceNameKey: 'services.staffing.title'
});
