{% from "macros/home-sections.njk" import contactForm with context %}

    <section class="page-hero">
        <div class="container">
            <h1>{{ "contact.title" | t(locale) }}</h1>
            <p>{{ "contact.subtitle" | t(locale) }}</p>
        </div>
    </section>

    {# Same id as the homepage anchor so /contact/#contact keeps working too. #}
    <section id="contact" class="page-section">
        <div class="container">
            {{ contactForm() }}
        </div>
    </section>
