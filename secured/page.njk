<header>
  <div class="brand"><span class="brand-base">Smart</span><span class="brand-accent">Agents</span></div>
  <div class="subtitle">Beveiligde documenten</div>
</header>

{% if securedSupportsProtectedDocs and securedFiles.length %}
<div class="list">
  {% for doc in securedFiles %}
  <div class="item">
    <span class="item-name">{{ doc.name }}</span>
    <div class="actions">
      {% if doc.html %}<a class="btn btn-view" href="{{ doc.html | withPathPrefix }}">Bekijken</a>{% endif %}
      {% if doc.pdf %}<a class="btn btn-pdf" href="{{ doc.pdf | withPathPrefix }}" download>PDF</a>{% endif %}
    </div>
  </div>
  {% endfor %}
</div>
{% elif not securedSupportsProtectedDocs %}
<div class="empty">
  Deze deployment draait in statische modus. Beschermde documenten worden daarom niet mee gepubliceerd.
  Gebruik een server-capabele deployment voor `/secured/`, of host deze documenten apart achter echte authenticatie.
</div>
{% else %}
<p class="empty">Geen documenten beschikbaar.</p>
{% endif %}
