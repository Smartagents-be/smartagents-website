<!-- ════════════════ 20 · LAB A1b · RESULT ════════════════ -->
  <section data-label="Lab A1b: result" class="slide light cat-themed" style="--c: #16a34a;">
    <div class="glow soft"></div>
    <div class="frame">
      <p class="eyebrow"><span class="num">20</span>Lab A1b result</p>
      <h2 class="h2">Ignoring the noise, in tokens.</h2>

      <div class="frame-body">
        <!-- TODO: replace 00,000 / 00% with the measured token counts (user will supply) -->
        <div style="display: grid; grid-template-columns: 1fr auto 1fr; align-items: center; gap: 40px; text-align: center;">
          <div>
            <div style="font-size: 23px; font-weight: 700; letter-spacing: 0.14em; text-transform: uppercase; color: #94a3b8;">Reads legacy/</div>
            <div style="font-size: 128px; font-weight: 800; line-height: 1; letter-spacing: -0.03em; color: #94a3b8;">466k</div>
            <div style="font-size: 20px; color: var(--fg-muted); margin-top: 6px;">unscoped, decoys in context</div>
          </div>
          <div style="font-size: 64px; line-height: 1; color: var(--c);">&rarr;</div>
          <div>
            <div style="font-size: 23px; font-weight: 700; letter-spacing: 0.14em; text-transform: uppercase; color: var(--c);">legacy/ ignored</div>
            <div style="font-size: 128px; font-weight: 800; line-height: 1; letter-spacing: -0.03em; color: var(--c);">351k</div>
            <div style="font-size: 20px; color: var(--fg-muted); margin-top: 6px;">one ignore rule, same fix</div>
          </div>
        </div>
        <p style="text-align: center; font-size: 30px; color: var(--fg-muted); margin: 40px 0 0;"><strong style="color: var(--c);">25%</strong> fewer tokens, just by fencing off one folder</p>
      </div>
    </div>
    {% set slideNumber = "20" %}
    {% set slideTotal = "62" %}
    {% include "../../shared/chrome.njk" %}
  </section>
