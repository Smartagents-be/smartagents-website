<!-- ════════════════ 05 · LAB 0: BASELINE ════════════════ -->
  <section data-label="Lab 0: baseline" class="slide light cat-themed" style="--c: #16a34a;">
    <div class="glow soft"></div>
    <div class="frame">
      <p class="eyebrow"><span class="num">05</span>Lab 0</p>
      <h2 class="h2">Measure your <span class="accent">baseline</span>.</h2>

      <div class="frame-body">
      <div class="labcard" style="margin-top: 20px;">
        <span class="lab-tag">Lab 0: baseline measurement</span>
        <h3>Fix the session-timeout bug, unoptimized</h3>
        <p style="font-size: 23px; color: var(--fg-muted); margin-bottom: 14px;"><strong>Objective:</strong> record your real token count and cost before any technique is applied. This is the number to beat in Labs A1 and B1.</p>
        <ol>
          <li>Open <code>token-efficient-workshop-node</code> in your AI coding tool.</li>
          <li>Ask it to fix the session-timeout bug with no scoping instruction, no ignore files, no focused context. Just the raw request.</li>
          <li>After the response, record: input tokens, output tokens, cached tokens, and total cost.</li>
        </ol>
        <p class="lab-out"><strong>Expected result:</strong> a baseline number that will shrink as you apply each technique. Keep it, you will compare it at the end of every track.</p>
      </div>      </div>
    </div>
    {% set slideNumber = "05" %}
    {% set slideTotal = "51" %}
    {% include "../../shared/chrome.njk" %}
  </section>
