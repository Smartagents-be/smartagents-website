<!-- ════════════════ 27 · SPECIALIZED AGENTS & MODES ════════════════ -->
  <section data-label="Agents & modes" class="slide light cat-themed" style="--c: #ea580c;">
    <div class="glow soft"></div>
    <div class="frame">
      <div class="cat-head">
        <span class="cat-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
        </span>
        <div class="cat-text">
          <p class="eyebrow"><span class="num">27</span>Extend</p>
          <h2 class="h2">Give a scoped task its <span class="accent">own agent</span>.</h2>
        </div>
      </div>

      <div class="frame-body">
      <div class="grid-3" style="margin-top: 36px; align-content: start;">

        <div class="card">
          <span class="lab"><i class="bi bi-copilot"></i>GitHub Copilot</span>
          <h3>Custom agents</h3>
          <ul>
            <li><code>.github/agents/*.agent.md</code> (formerly "chat modes").</li>
            <li>Built-in: Ask, Edit, Agent, plus a separate Plan agent.</li>
          </ul>
        </div>

        <div class="card">
          <span class="lab"><i class="bi bi-claude"></i>Claude Code</span>
          <h3>Subagents</h3>
          <ul>
            <li>Its own system prompt, tools and context per task (<code>.claude/agents</code>).</li>
            <li>Keeps the main context small; runs tasks in parallel.</li>
          </ul>
        </div>

        <div class="card">
          <span class="lab"><i class="bi bi-cursor"></i>Cursor</span>
          <h3>Custom modes + subagents</h3>
          <ul>
            <li>Its own tool set, model and instructions per mode (beta).</li>
            <li>Built-in: Agent, Ask, Plan.</li>
          </ul>
        </div>

      </div>

      <div class="callout">
        A specialized agent only sees what it needs. <strong>Less noise, sharper output</strong>.
      </div>
      </div>
    </div>
    {% set slideNumber = "27" %}
    {% set slideTotal = "61" %}
    {% include "../../shared/chrome.njk" %}
  </section>
