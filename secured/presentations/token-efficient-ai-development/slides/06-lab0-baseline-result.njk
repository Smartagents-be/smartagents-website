<!-- ════════════════ 06 · LAB 0 · BASELINE RESULT ════════════════ -->
  <section data-label="Lab 0: baseline result" class="slide light cat-themed" style="--c: #16a34a;">
    <div class="glow soft"></div>
    <div class="frame">
      <p class="eyebrow"><span class="num">06</span>Lab 0 result</p>
      <h2 class="h2">The baseline, <span class="accent">in one number</span>.</h2>

      <div class="frame-body">
        <div style="text-align: center;">
          <!-- TODO: replace 00,000 with the measured baseline token count (user will supply) -->
          <div style="font-size: 220px; font-weight: 800; line-height: 1; letter-spacing: -0.03em; color: var(--c);">00,000</div>
          <p style="font-size: 30px; color: var(--fg-muted); margin: 20px 0 0;">tokens to fix the session-timeout bug, unoptimized</p>
          <p style="font-size: 20px; color: #94a3b8; margin: 10px 0 0;">Placeholder. The measured baseline goes here, the number every technique has to beat.</p>
        </div>
      </div>
    </div>
    {% set slideNumber = "06" %}
    {% set slideTotal = "51" %}
    {% include "../../shared/chrome.njk" %}
  </section>
