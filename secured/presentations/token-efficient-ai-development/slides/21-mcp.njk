<!-- ════════════════ 21 · MCP ════════════════ -->
  <section data-label="MCP" class="slide light cat-themed" style="--c: #9333ea;">
    <div class="glow soft"></div>
    <div class="frame">
      <div class="cat-head">
        <span class="cat-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><line x1="8.59" y1="13.51" x2="15.42" y2="17.49"/><line x1="15.41" y1="6.51" x2="8.59" y2="10.49"/></svg>
        </span>
        <div class="cat-text">
          <p class="eyebrow"><span class="num">21</span>Extend</p>
          <h2 class="h2">Connect the agent to <span class="accent">external tools and data</span>.</h2>
        </div>
      </div>

      <div class="frame-body">
      <div class="grid-3" style="margin-top: 36px; align-content: start;">
        <div class="card">
          <span class="lab">Claude Code</span>
          <h3>MCP</h3>
          <ul>
            <li>Servers in <code>.mcp.json</code> or via <code>claude mcp add</code>.</li>
            <li>Manage with <code>/mcp</code>.</li>
          </ul>
        </div>
        <div class="card">
          <span class="lab">Cursor</span>
          <h3>MCP</h3>
          <ul>
            <li><code>.cursor/mcp.json</code> or globally.</li>
            <li>Settings &rarr; MCP to manage.</li>
          </ul>
        </div>
        <div class="card">
          <span class="lab">GitHub Copilot</span>
          <h3>MCP</h3>
          <ul>
            <li><code>.vscode/mcp.json</code> in the workspace.</li>
            <li>Agent mode uses the servers.</li>
          </ul>
        </div>
      </div>

      <div class="callout">
        One open standard gives every agent access to the same tools and data. Note: <strong>every server costs context</strong> (see slide 03).
      </div>
      </div>
    </div>
    {% set slideNumber = "21" %}
    {% set slideTotal = "41" %}
    {% include "../../shared/chrome.njk" %}
  </section>
