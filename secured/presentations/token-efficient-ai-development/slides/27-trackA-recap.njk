<!-- ════════════════ 27 · TRACK A RECAP ════════════════ -->
  <section data-label="Track A recap" class="slide light cat-themed" style="--c: #16a34a;">
    <div class="glow soft"></div>
    <div class="frame">
      <div class="cat-head">
        <span class="cat-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
        </span>
        <div class="cat-text">
          <p class="eyebrow"><span class="num">27</span>Track A recap</p>
          <h2 class="h2">What the editor track taught us.</h2>
        </div>
      </div>

      <div class="frame-body">
      <ul class="checklist" style="margin-top: 28px; font-size: 22px; gap: 16px;">
        <li>Scope to the smallest high-signal set: only the files that matter, not the whole repo.</li>
        <li>One focused task per session; compact in time so context stays fresh.</li>
        <li>Capture conventions once in an instruction file and never repeat them in a prompt.</li>
        <li>Let skills and subagents do scoped work so the main context stays small.</li>
        <li>Automate guardrails with hooks: format, test, and block without a token spent.</li>
        <li>Add only the MCP servers you genuinely need; every idle server costs context.</li>
      </ul>      </div>
    </div>
    {% set slideNumber = "27" %}
    {% set slideTotal = "51" %}
    {% include "../../shared/chrome.njk" %}
  </section>
